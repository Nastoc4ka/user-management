import React from 'react';
import './dashboard.css';
import {Alert} from "react-bootstrap";

const Dashboard = () => {

    return (
        <div>
            <Alert variant='success'>Dashboard</Alert>
        </div>
    )
};

export default Dashboard;